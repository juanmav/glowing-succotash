{
  "name": "glowing-succotash",
  "version": "1.0.0",
  "description": "Car Data Scraper — Chrome extension + backend",
  "private": true,
  "workspaces": ["backend", "extension", "mock-loan-site"],
  "scripts": {
    "build": "npm run build --workspace=backend && npm run build --workspace=extension",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:extension": "npm run dev --workspace=extension",
    "dev:mock-loan": "npm run dev --workspace=mock-loan-site",
    "generate-icons": "node scripts/generate-icons.js"
  }
}
